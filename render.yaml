services:
  - type: web
    name: pdfmaster-ultra-suite
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    autoDeploy: true
    envVars:
      - key: APP_CONFIG
        value: production
      - key: FLASK_DEBUG
        value: "false"
